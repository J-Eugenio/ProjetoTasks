/**
 * @format
 */

import {AppRegistry} from 'react-native';
import nagivator from './src/nagivator';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => nagivator);
